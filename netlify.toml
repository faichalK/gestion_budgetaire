[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Toutes les routes → index.html (SPA React Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
